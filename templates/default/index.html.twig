{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur Épices du Maroc{% endblock %}

{% block body %}
<section class="pt-5 pb-3 bg-light">
    <div class="container text-center">
        <h1 class="display-4">Épices du Maroc</h1>
        <p class="lead">Explorez notre vitrine virtuelle d'épices authentiques et d'herbes marocaines, une célébration des saveurs qui définissent la cuisine traditionnelle.</p>
        <p>Découvrez notre sélection de 100% d'épices naturelles et d'herbes aromatiques, directement importées du Maroc.</p>
    </div>
</section>

<section class="category-highlights my-5">
    <div class="container">
        <div class="row row-cols-1 row-cols-md-3 g-4">
            <div class="col">
                <div class="card">
                    <img src="{{ asset('images/epices.jpg') }}" class="card-img-top category-image" alt="Épices">
                    <div class="card-body">
                        <h5 class="card-title">Épices</h5>
                        <p class="card-text">Explorez notre gamme diversifiée d'épices marocaines.</p>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="card">
                    <img src="{{ asset('images/herbes.jpg') }}" class="card-img-top category-image" alt="Herbes">
                    <div class="card-body">
                        <h5 class="card-title">Herbes</h5>
                        <p class="card-text">Découvrez les herbes qui parfument la cuisine marocaine.</p>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="card">
                    <img src="{{ asset('images/recettes.jpg') }}" class="card-img-top category-image" alt="Recettes">
                    <div class="card-body">
                        <h5 class="card-title">Recettes</h5>
                        <p class="card-text">Trouvez l'inspiration avec nos recettes à base d'épices et d'herbes.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- Custom CSS for controlling image size and clarity -->
<style>
    .category-image {
        height: 150px;  /* Adjusted for better proportionality */
        object-fit: cover; /* Keeps the aspect ratio, crops the image */
        width: 100%; /* Ensures the image covers the card top area */
    }
</style>

<section class="freshness-section py-5 bg-white">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6">
                <img src="{{ asset('images/tout.jpg') }}" class="img-fluid" alt="Épices Traditionnels" style="max-height: 400px; width: 100%; object-fit: cover;">
            </div>
            <div class="col-md-6">
                <h2 class=" display-4">De la fraîcheur dans vos assiettes</h2>
                <p>Vous êtes sur le point d'entrer dans le monde fascinant et aromatique des épices marocaines. Sur Épices du Maroc, nous nous efforçons de vous apporter un peu de notre humble passion pour les épices et les herbes qui font de la cuisine marocaine un voyage culinaire inoubliable.

                    Chaque produit est minutieusement sélectionné pour sa qualité et sa fraîcheur. De plus, nous prenons soin de moudre et de préparer nos épices au moment de la préparation de vos commandes, afin que vous profitiez de leur saveur et de leur parfum au maximum.

                    Nos épices sont destinées à sublimer vos plats et à vous offrir une expérience sensorielle unique. Pour cela, notre équipe travaille avec amour pour partager avec vous la richesse de la gastronomie marocaine.</p>
            </div>
        </div>
    </div>
</section>

    <section class="featured-products py-5">
        <div class="container">
            <h2   class="text-center mb-4 display-4">Produits Phares</h2>
            <p class="lead text-center mb-5" style="font-size: 1.2em; max-width: 800px; margin: auto; line-height: 1.5; color: #666;">Les épices les plus demandées et fréquemment utilisées dans la gastronomie qui remportent un vif succès tant au niveau des ventes que de leur utilisation.</p>
            <div id="productsCarousel" class="carousel slide" data-bs-ride="carousel">

                <ol class="carousel-indicators">
                    {% for product in featured_products %}
                        <li data-bs-target="#productsCarousel" data-bs-slide-to="{{ loop.index0 }}" class="{{ loop.first ? 'active' : '' }}"></li>
                    {% endfor %}
                </ol>

                <!-- Wrapper for slides -->
                <div class="carousel-inner">
                    {% for product in featured_products %}
                        <div class="carousel-item {{ loop.first ? 'active' : '' }}">
                            <img src="{{ asset('images/' ~ product.image) }}" class="d-block w-100" alt="{{ product.nom }}" style="max-height: 320px; object-fit: cover;">
                            <div class="carousel-caption d-none d-md-block">
                                <h5>{{ product.nom }}</h5>
                                <p>{{ product.description }}</p>
                            </div>
                        </div>
                    {% else %}
                        <div class="carousel-item active">
                            <p class="text-center">Aucun produit vedette à afficher pour le moment.</p>
                        </div>
                    {% endfor %}
                </div>

                <!-- Controls -->
                <a class="carousel-control-prev" href="#productsCarousel" role="button" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </a>
                <a class="carousel-control-next" href="#productsCarousel" role="button" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </a>
            </div>
        </div>
    </section>
    <section class="about-us py-5 bg-light">
        <div class="container">
            <h2 class="text-center mb-4 display-4">Qui Sommes Nous ?</h2>
            <p class="lead text-center mb-5" style="font-size: 1.2em; max-width: 800px; margin: auto; line-height: 1.5; color: #ffffff;">
                Épices du Maroc est une entreprise familiale spécialisée dans la vente d'épices provenant du Maroc. Nous avons sélectionné avec exigence nos épices et herbes afin de vous les proposer au meilleur rapport qualité-prix. Soucieux de l'environnement et portant un réel intérêt à consommer des aliments sains, tous nos produits sont issus de l'agriculture biologique. Vous trouverez également sur notre site des conseils d'usage, les bienfaits ainsi que des idées recettes et aurez la possibilité d'acheter des accessoires pour épices.
            </p>
        </div>
    </section>

    <!-- Custom CSS for enhancing the style -->
    <style>
        .about-us {
            background: url('https://t3.ftcdn.net/jpg/06/06/23/96/240_F_606239697_iEsNYH9HHd3TKR5aoXwvUYq3fEHmDpnQ.jpg') no-repeat center center;
            background-size: cover;
            color: #ffffff; /* Adjust text color based on your background for readability */
        }

        .about-us .container {
            background: rgba(0, 0, 0, 0.5); /* Optional: Adds a dark overlay to ensure text readability */
            border-radius: 7px;
            padding: 25px;
        }
    </style>
    <section class="new-arrivals py-5 bg-light">
        <div class="container">
            <h2 class="text-center mb-4 display-4">C'est Nouveau !!</h2>
            <p class="lead text-center mb-5" style="font-size: 1.2em; max-width: 800px; margin: auto; line-height: 1.5; color: #666;">
                Découvrez les dernières additions à notre gamme d'épices et herbes fraîchement importées du Maroc.
            </p>
            <div id="newArrivalsCarousel" class="carousel slide" data-bs-ride="carousel">
                <ol class="carousel-indicators">
                    {% for product in new_arrivals %}
                        <li data-bs-target="#newArrivalsCarousel" data-bs-slide-to="{{ loop.index0 }}" class="{{ loop.first ? 'active' : '' }}"></li>
                    {% endfor %}
                </ol>
                <div class="carousel-inner">
                    {% for product in new_arrivals %}
                        <div class="carousel-item {{ loop.first ? 'active' : '' }}">
                            <img src="{{ asset('images/' ~ product.image) }}" class="d-block w-100" alt="{{ product.nom }}" style="max-height: 320px; object-fit: cover;">
                            <div class="carousel-caption d-none d-md-block">
                                <h5>{{ product.nom }}</h5>
                                <p>{{ product.description }}</p>
                            </div>
                        </div>
                    {% else %}
                        <div class="carousel-item active">
                            <p class="text-center">Aucune nouveauté à présenter pour le moment.</p>
                        </div>
                    {% endfor %}
                </div>
                <a class="carousel-control-prev" href="#newArrivalsCarousel" role="button" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </a>
                <a class="carousel-control-next" href="#newArrivalsCarousel" role="button" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span the="visually-hidden">Next</span>
                </a>
            </div>
        </div>
    </section>

    <div class="container py-4">
        <h1 class="text-center mb-3">Nos Recettes</h1>
        <div class="row">
            {% for recette in recettes %}
                <div class="recette">
                    <h2>{{ recette.nom }}</h2>
                    <p>{{ recette.description | slice(0, 100) ~ '...' }}</p>
                    <a href="{{ path('recette_detail', {'id': recette.id}) }}" class="btn btn-primary">Voir plus</a>
                </div>

            {% else %}
                <div class="col-12">
                    <p class="text-center">Aucune recette disponible.</p>
                </div>
            {% endfor %}
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2-dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}
